## Compose variables (optional)
# Copy to .env to override defaults:
#   cp .env.example .env

# Host ports
FRONTEND_PUBLISH_PORT=80
API_PUBLISH_PORT=8081

# Frontend build-time API URL (Vue)
VUE_APP_API_URL=http://localhost:8081

# Image naming
IMAGE_PREFIX=local
TAG=local
